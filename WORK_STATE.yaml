work_package: archaeon-ai/sherloc-pipeline#38 (hydration fitting: cosmic rays fit as OH-stretch peaks — CR veto across all implementations)
branch: 38-hydration-fitting-cosmic-rays-fit-as-oh-st-attempt2
head: 19a3124 (uncommitted working tree; runner commits)
open_decisions:
  - "REQ-5: threshold ratification is the operator's. Proposed values in docs/reports/HYDRATION_CR_VETO_EVIDENCE.md; flag stays default OFF until ratified on the issue."
  - "REQ-4: the corpus sweep over the floor-pinned rows + the reproduction line scan needs the team-tier DB, unreachable from this repo's environment. Delivered as the read-only scripts/hydration_cr_veto_report.py; numbers still to be produced by the operator."
next_action: operator runs scripts/hydration_cr_veto_report.py against the team DB, records catch/overlap/false-positive counts on issue #38, then ratifies (or retunes) the thresholds before any default-on flip
last_verified: 2026-09-03 — full pytest suite in this worktree, 3441 passed / 225 skipped / 2 deselected. The 2 deselected (tests/regression/test_golden_pipeline.py::TestGoldenBaselineRegression::test_review_decisions_match[ml|modz]) fail identically on a clean detached worktree of branch head 19a3124, so the failure pre-exists this change.
