work_package: archaeon-ai/sherloc-pipeline#38 (hydration fitting: cosmic rays fit as OH-stretch peaks — CR veto across all implementations)
branch: 38-hydration-fitting-cosmic-rays-fit-as-oh-st-attempt2
head: 19a3124 (uncommitted working tree; runner commits)
open_decisions:
  - "REQ-5: threshold ratification is the operator's. Proposed values in docs/reports/HYDRATION_CR_VETO_EVIDENCE.md; flag stays default OFF until ratified on the issue."
  - "REQ-4: the corpus sweep over the floor-pinned rows + the reproduction line scan needs the team-tier DB, unreachable from this repo's environment. Delivered as the read-only scripts/hydration_cr_veto_report.py; numbers still to be produced by the operator."
next_action: operator runs scripts/hydration_cr_veto_report.py against the team DB, records catch/overlap/false-positive counts on issue #38, then ratifies (or retunes) the thresholds before any default-on flip
last_verified: 2026-09-03 — review round 2 addressed. (1) Stale artifacts: _fit_point_hydration now deletes its per-point peaks CSV + overlay PNG on every no-detection exit, fit_hydration deletes the accepted-peaks summary when the scan accepts nothing, and persist_raman_peaks treats "scan summary present, no per-point CSVs" as a zero result that clears the domain's existing DB rows (only a never-fitted domain still raises). (2) Despike consistency: all callers — pipeline, map mode (veto AND fit preprocessing), web point fit, web modz preview, evidence sweep (new --config) — build DespikeParams via the single new core.preprocessing.despike_params_from_config(), which resolves every field including run_length_max and the guard windows. New tests: off-then-on rerun at worker, service (tests/unit/services/test_hydration_rerun_artifacts.py) and persistence levels, plus a non-default preprocessing.despike override asserted across all three veto paths. Suites in this worktree: unit 2996 passed / 17 skipped; integration 287 passed / 206 skipped; contract+regression 177 passed / 2 skipped with the same 2 pre-existing test_review_decisions_match[ml|modz] golden failures (amplitude rtol 1e-4 drift) that fail identically on clean branch head 19a3124. No golden fixture churn.
